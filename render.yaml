services:
  - type: web
    name: desi-prompts-backend
    env: node
    buildCommand: npm run build
    startCommand: npm start
    nodeVersion: 22
    envVars:
      - key: NODE_ENV
        value: production