services:
  - type: web
    name: desi-prompts-backend
    env: node
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm start
    nodeVersion: 22
    envVars:
      - key: NODE_ENV
        value: production